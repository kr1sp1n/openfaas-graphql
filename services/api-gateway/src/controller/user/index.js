module.exports = ({ db }) => {
  // const User = db.model('User', {
  //   email: {
  //     type: String,
  //     required: true,
  //     unique: true,
  //   }
  // })
  return {
    // registerUser: async ({ email }, context) => {
    //   const user = await User.create({ email })
    //   return user.id
    // }
  }
}
